pipeline {
    agent any
    options{
        timestamps()
    }
    triggers{
        pollSCM('* * * * *')
    }
    stages {
        stage("Cron Task") {
            steps {
                echo "Hey, There!"
                git url:"git@github.com:vinoth4180/devops-class.git"
            }
        }
    }
}
